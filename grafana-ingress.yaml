apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: grafana
  namespace: monitoring
  labels:
    app: grafana
    component: core
spec:
  rules:
  - host: grafana.host.com
    http:
      paths:
      - path: /
        backend:
          servicePort: 80
          serviceName: grafana
